(function(){function resize(file){return new Promise(function(resolve,reject){var fr=new FileReader();fr.onerror=function(){reject(new Error("图片读取失败"))};fr.onload=function(){var img=new Image();img.onerror=function(){reject(new Error("图片解码失败"))};img.onload=function(){var max=2200,s=Math.min(1,max/Math.max(img.naturalWidth||img.width,img.naturalHeight||img.height)),w=Math.round((img.naturalWidth||img.width)*s),h=Math.round((img.naturalHeight||img.height)*s),c=document.createElement("canvas");c.width=w;c.height=h;var ctx=c.getContext("2d");if(!ctx)return reject(new Error("浏览器不支持图片处理"));ctx.fillStyle="#fff";ctx.fillRect(0,0,w,h);ctx.drawImage(img,0,0,w,h);c.toBlob(function(blob){if(!blob)return reject(new Error("图片压缩失败"));resolve(new File([blob],"schedule.jpg",{type:"image/jpeg"}))},"image/jpeg",.86)};img.src=fr.result};fr.readAsDataURL(file)})}window.recognize=async function(file){var box=document.getElementById("recognitionResult");if(!file){if(box)box.innerHTML="<p class=\"err\">还没有选择课程表图片。</p>";return}try{if(box)box.innerHTML="<div class=\"recognition-status\"><b>① 图片已选择</b><span>正在压缩并准备上传…</span></div>";var small=await resize(file);if(box)box.innerHTML="<div class=\"recognition-status\"><b>② 图片已准备</b><span>正在调用智谱视觉 API…</span></div>";var reader=new FileReader();reader.onload=async function(){try{if(box)box.innerHTML="<div class=\"recognition-status\"><b>③ 已上传</b><span>AI 正在识别课程表，请稍候…</span></div>";var d=await api("/zhipu",{method:"POST",body:JSON.stringify({image:reader.result,prompt:"识别课程表并只返回严格 JSON：{courses:[{title,day,start,end,periods,room}]}。只识别图片中实际出现且能确认的日期，课程可能只有1到7天，绝对不要补齐不存在的日期。day只能是周一到周日。明确时钟时间优先；连续节次合并为真实开始和结束时间；看不清的信息不要猜。"} )});if(!d||!d.choices||!d.choices.length){var em="视觉 API 没有返回结果";if(d&&d.error){if(d.error.message)em=d.error.message;else if(typeof d.error==="string")em=d.error}throw new Error(em)}var raw=d.choices[0]&&d.choices[0].message&&d.choices[0].message.content||"",clean=String(raw).trim(),a=clean.indexOf("{"),b=clean.lastIndexOf("}"),data=null;if(a>=0&&b>a)data=JSON.parse(clean.slice(a,b+1));else data=JSON.parse(clean);var list=Array.isArray(data.courses)?data.courses:[],days=[];list.forEach(function(v){if(v.day&&days.indexOf(v.day)<0)days.push(v.day)});if(!list.length)throw new Error("API 已返回，但没有识别到可确认的课程");var html="<div class=\"recognition-head\"><div><b>④ 识别成功</b><span> 共 "+days.length+" 天、"+list.length+" 门/节课程</span></div><button id=\"clearRecognition\" class=\"secondary\" type=\"button\">清除</button></div><p class=\"muted\">已确认图片上传成功、智谱视觉 API 返回成功，并解析出课程。没有自动补齐其他天。</p><div class=\"recognition-days\">";days.forEach(function(day){html+="<div class=\"recognition-day\"><b>"+esc(day)+"</b>";list.filter(function(v){return v.day===day}).forEach(function(v){html+="<div class=\"recognition-course\"><span>"+esc(v.start||"--")+"–"+esc(v.end||"--")+"</span><strong>"+esc(v.title||"未命名课程")+"</strong>";if(v.room)html+="<small>"+esc(v.room)+"</small>";html+="</div>"});html+="</div>"});html+="</div>";if(box)box.innerHTML=html;var clear=document.getElementById("clearRecognition");if(clear)clear.onclick=function(){box.innerHTML=""}}catch(e){if(box)box.innerHTML="<div class=\"recognition-status error\"><b>识别失败</b><span>"+esc(e.message||"未知错误")+"</span></div><p class=\"muted\">如果这里显示 API 错误，说明图片已经成功上传，问题在视觉接口或返回内容，而不是按钮。</p>"}};reader.onerror=function(){if(box)box.innerHTML="<p class=\"err\">图片读取失败。</p>"};reader.readAsDataURL(small)}catch(e){if(box)box.innerHTML="<div class=\"recognition-status error\"><b>上传前失败</b><span>"+esc(e.message||"未知错误")+"</span></div>"}}};function bind(){var i=document.getElementById("image");if(i)i.onchange=function(e){window.recognize(e.target.files[0])}}if(document.readyState==="loading")document.addEventListener("DOMContentLoaded",bind);else bind()})();